# Agent Skills

This directory contains autonomous workflows and skills for Antigravity. When a skill is placed here inside a folder with a `SKILL.md` file containing YAML frontmatter (name and description), the IDE automatically discovers and trigger-matches it against user intents.
