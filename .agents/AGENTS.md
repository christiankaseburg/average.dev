# Master Agent System Configuration

## 🤖 Rule Discovery Directive
You are commanded to proactively read, register, and adhere to all domain-specific constraint guidelines contained within the `.agents/rules/` directory before writing code or modifying project files.

## 🛑 Global Constraints & Boundaries
1. Always parse files located in `.agents/rules/` matching the specific workspace tech stack you are interacting with.
2. Read the global `agents.md` file in the project root to map out business logic expectations before writing new features.
3. Never bypass local project compiler warnings. If a terminal task produces an error, automatically spin up an internal debugging cycle.
